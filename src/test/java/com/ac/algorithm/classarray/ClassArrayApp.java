package com.ac.algorithm.classarray;

/**
 * 测试对象数组
 */

public class ClassArrayApp {

    /*public static void main(String[] args) {

        int maxSize =100;

        ClassArray arr = new ClassArray(maxSize);

        arr.insert("chao","an",25);
        arr.insert("chao2","an",28);
        arr.insert("chao3","an",30);
        arr.insert("chao4","an",18);
        arr.insert("chao5","an",40);
        arr.insert("chao6","an",27);
        arr.insert("chao7","an",16);
        arr.insert("chao8","an",34);
        arr.insert("chao9","an",23);
        arr.insert("chao10","an",38);
        arr.displayP();
        String searchKey ="chao7";
        Person found;
        found = arr.find(searchKey);
        if(found!=null){
            System.out.println("Found ");
            found.dispalyPerson();
        }else{
            System.out.println("Not Found ");
        }
    System.out.println("Delting chao2,chao3,chao4");
        arr.delete("chao2");
        arr.delete("chao3");
        arr.delete("chao4");
        arr.displayP();
    }*/
}
